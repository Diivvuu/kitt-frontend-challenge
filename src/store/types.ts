// src/store/types.ts
export interface FlightState {
    selectedFrom: string; // Adjust the type according to your data
    selectedTo: string; // Adjust the type according to your data
    departureDate: string; // Adjust the type according to your data
    returnDate: string; // Adjust the type according to your data
  }
  
  // Add other state interfaces if necessary
  